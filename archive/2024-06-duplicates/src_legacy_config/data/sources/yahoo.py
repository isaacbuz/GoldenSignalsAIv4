def fetch_headlines(ticker: str) -> list:
    """Mock: Returns sample headlines for the given ticker."""
    return [
        f"{ticker} surges after earnings beat expectations",
        f"Analysts debate future of {ticker} options strategies",
        f"{ticker} volatility rises amid market uncertainty"
    ]

